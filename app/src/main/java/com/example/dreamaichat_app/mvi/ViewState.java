package com.example.dreamaichat_app.mvi;

/**
 * ViewState 接口
 * 
 * MVI 架构中的 ViewState 表示视图的状态
 * 包含 UI 需要显示的所有数据
 * 
 * ViewState 应该是不可变的（Immutable）
 */
public interface ViewState {
    // ViewState 是一个标记接口，具体的 ViewState 由各个模块定义
}

