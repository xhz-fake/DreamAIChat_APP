# 邢浩哲 - DreamAIChat 界面设计白皮书 - v2.0

## - "Dream" 对话流界面逻辑设计



## 界面总览

- ```
    LoginScreen (登录界面)
    
    ↓
    
    MainActivity (主容器)
    └─── ChatScreen (聊天界面)
    
     	└── Sidebar (侧边栏)
    
      		├── TemplateSelectionScreen (模板选择)
      		├── ModelRoutingScreen (模型路由)
      		├── ConversationGraphScreen (对话图谱)
       		├── HistoryScreen (历史界面)
            └── ProfileScreen (个人界面)
    
    - 下列每个界面中的a,b,c,...表示自上而下排布的模块
    - 界面名,图片名等尽可能使用中文直译英文便于理解并使用驼峰命名法
    - 每个用户的信息应当单独存储, 登陆后是每个用户个性的相关设置和历史
    ```

    



## 界面一: LoginScreen - 登录界面

**功能**：用户认证和品牌展示入口

**布局模块**：

- a. **品牌展示区** (BrandSection)
    - 应用Logo图标 + "Dream"文字
    - 副标题："更强大的AI助手"
    - 品牌色：青绿色渐变背景
- b. **登录表单区** (LoginForm)
    - b1. 手机号/邮箱输入框
        - 提示文字："请输入手机号或邮箱"
        - 前缀图标：📱
    - b2. 密码输入框  
        - 提示文字："请输入密码"
        - 前缀图标：🔒
        - 右侧：显示/隐藏密码按钮(👁️)
    - b3. "登录"按钮
        - 点击验证账号密码是否正确, 如正确则跳转对应的(界面二), 并加载该账号对应历史记录
    - b4. "注册账号"文字按钮
        - 点击后检查输入的账号是否存在, 如不存在则创建并跳转至(界面二)
- c. **底部信息区** (Footer)
    - "登录即代表同意《用户协议》和《隐私政策》"
    - 可点击链接，弹出协议内容



## 界面二: MainActivity - 主界面

**功能**：核心交互区

**布局模块**：

- a. **顶部功能栏** (TopFunctionBar)
    - 左侧：菜单按钮及图标
        - 点击按钮或者直接右滑主界面以打开侧边栏(界面三),
    - 中间：应用名称"Dream" + 当前模型状态指示器
    - 右侧：开启新对话按钮及图标
        - 点击跳转(界面二), 将上一个对话存入历史对话并开启一个新对话
- b. **中央文本交互区域** (CenterContentArea)
    - **功能**：核心对话交互界面
    - **布局模块**：(自上而下)
        - b1. **对话状态栏** (ChatStatusBar)
            - 显示文本："Dream · 在线"
        - b2. **消息列表区域** (MessageList)
            - 使用LazyColumn实现可滚动列表
            - 消息项类型：
                - 用户消息：右侧对齐，淡绿色背景
                - AI消息：左侧对齐，浅灰色背景
                - 系统消息：居中，半透明背景
            - 每条消息包含：
                - 头像（用户👤 / AI🤖）
                - 消息气泡（支持文本和图片）
                - 时间戳（HH:mm格式）
                - 消息状态指示图标（发送中/成功/失败）
- c. **最底部输入及功能栏** (BottomNavigation)
    - c1. 输入框 (默认选中)
        - 未输入时默认文字："给 Dream 发送信息: "
        - 支持多行文本输入
        - 最大高度限制，可滚动
        - 点击弹出键盘(默认支持中文输入法)
    - c2. 功能条
        - c2_1(功能条最左侧): 模型选择按钮(有图标和"模型"文字)
            - 点击后弹出模型选择轮盘可以选择默认的几个模型(暂时提供: 豆包, DeepSeek)
        - c2_2(功能条最右侧): 发送信息按钮
            - 点击后发送输入的信息并在聊天主界面显示 
            - 默认状态：发送图标
            - 加载状态：沙漏加载图标
        - c2_3(c2_2的左侧): 附件添加按钮(加号图标) 
            - 点击弹出附件小菜单：
                - c2_3_1: 相册按钮及图标
                    - 点击后弹出(界面七)
                - c2_3_2: 拍照按钮及图标
                    - 当前版本不开发此功能



## 界面三: Sidebar - 侧边栏

**功能**：特色功能快速访问和历史记录浏览

**布局模块**(自上而下)：

- a. 顶部快速操作栏
    - a1. 操作栏左侧: 返回主界面按钮(返回箭头图标)
        - 点击后返回(界面二) 
- b. **用户信息栏** (UserHeader)
    - 包含用户头像 + 账号
    - 点击后弹出设置界面(界面四)
- c. **特色功能区** (FeatureSection)
    - c1. 模板系统按钮 
        - 图标：📝
        - 文本："对话模板"
        - 描述："快速开始专业对话"
        - 点击跳转(界面八)
    - c2. 路由系统卡片按钮
        - 图标：🔄  
        - 文本："智能路由"
        - 描述："优化模型选择"
        - 点击跳转(界面九)
    - c3. 对话图谱系统按钮
        - 图标：🕸️
        - 文本："对话图谱"
        - 描述："可视化对话关系"
        - 点击跳转(界面十)
- d. **历史记录区** (HistorySection)
    - d1. 所有的历史记录快览
        - 每条历史记录显示: 对话梗概 + 时间  
            - 点击对应的历史记录展示对应对话内容(界面二)



## 界面四: SettingsScreen - 设置界面

**功能**：基础功能设置

**布局模块**：

- a. **返回导航**
    - 左上角返回按钮, 图标为箭头
        - 点击后跳转(界面三)
    
- b. 账户栏(文本为"账户")
    - b1. 账号设置按钮
        - 按钮最左侧: 图标
        - 按钮中央: "账号设置"文本
        - 点击后跳转(界面六)
    - b2. 数据管理按钮 (StatsCard)
        - 按钮最左侧: 图标
        - 按钮中央: "数据管理"文本
        - 点击后跳转(界面五)
    
- c. 关于栏(文本为"关于")
    - c1. 检查更新按钮
        - 按钮最左侧: 图标
        - 按钮中央: "检查更新"文本
        - 点击显示当前版本
    - c2. 隐私条例按钮
        - 按钮最左侧: 图标
        - 按钮中央: "隐私条例"文本
        - 点击展示隐私条例界面
    
- d. 退出登录按钮
    - 按钮最左侧: 图标
    
    - 按钮中央: "退出登录"文本
    
    - 点击后展示提示窗
        - 标题: 退出登录
        
        - 中央文本: 退出登录不会丢失任何数据, 你仍可以登录此账号
        
        - 下方左右两个按钮:
            - d1. 左侧: 取消
                - 点击后跳转(界面四)
                
            - d2. 右侧: 确认退出
                - 点击后返回(界面一)
                
                

## 界面五: DataSettings - 数据管理

**功能**：管理用户数据

**布局模块**：

- a. **返回导航**
    - 左上角返回按钮, 图标为箭头
        - 点击后跳转(界面四)
    
- b. 删除历史记录按钮
    - 无图标, 文本:"删除历史记录" , 红色字体
        - 点击后弹窗:
            - 文本: 是否彻底删除所有历史记录? 该操作不可逆转!
            
            - 下方两个按钮: 
                - b1. 取消
                    - 点击后返回(界面五) 
                    
                - b2. 确认
                    - 点击后删除所有本地历史记录并跳转(界面二)
                    
                    



## 界面六: AccountSettings - 账号设置

**功能**：账号设置和个性化设置

**布局模块**：

- a. **返回导航:**
    - 左上角返回按钮, 图标为箭头
        - 点击后跳转(界面四)
- b. 账户栏:
    - b1. 手机号按钮
        - 按钮最左侧为图标
        - 中间为文本:"手机号"
        - 最右侧为登陆时的账号信息即手机号
- c. 注销账号按钮
    - 红色字体, 按钮仅有文本:"注销账号"
        - 点击后弹窗:
            - c1. 弹窗文本: 是否注销账号? 该操作不可逆转! 
            - c2. 下方的两个按钮:
                - 左侧: "取消"按钮, 点击后返回(界面六)
                - 右侧: "确认"按钮, 点击后将彻底清除账号的所有信息, 返回(界面一)



## 界面七: PhotosChoose - 图片选择

**功能**：添加多个系统的图片提交给模型, 模型提取文字并回答问题

- 进入界面后展示安卓系统提供的图片添加界面, 选择照片不超过三张, 点击添加后提交给当前的模型, 跳转(界面二) , 界面二的输入框上展示选中图片的图标, 名称 , 以及是否识别成功的状态, 如果某个图片上传失败则它的图标直接消失即可, 如果全部上传成功点击发送后, 输入框上面的一系列图标全部清除.



## 界面八: TemplateSelectionScreen - 模板选择界面

**功能**：智能对话模板选择和应用

**布局模块**：

- a. **返回导航**
    - 左上角返回箭头按钮
        - 点击后返回(界面三)
    - 标题："对话模板"
- b. **模板网格** (TemplateGrid)
    - 2列网格布局
    - 模板卡片包含：
        - 图标（预设对应图标）
        - 模板名称
        - 简短描述
        - 使用场景标签
    - 预设4个核心模板：
        - b1: **编程助手** 💻
            - 描述："代码编写和调试助手"
        - b2: **论文助手** 📚
            - 描述："学术写作和研究指导"  
        - b3: **医疗咨询** 🏥
            - 描述："健康咨询和医疗建议"
        - b4: **翻译专家** 🌐
            - 描述："多语言翻译和本地化"
- c. **模板详情浮层** (TemplateDetail)
    - 点击某模板后显示
        - c1. 详细功能介绍
        - c2. 使用示例
        - c3. "使用此模板"按钮 → 应用模板并返回(界面二)



## 界面九: ModelRoutingScreen - 模型路由界面

**功能**：多模型路由策略配置

**布局模块**：

- a. **返回导航**
    - 左上角返回箭头按钮
        - 点击后返回(界面三)
    - 标题："智能路由"
- b. **路由模式****选择** (RoutingMode)
    - 单选按钮组：
        - b1: **自动模式** (默认选中)
            - 描述："系统智能选择最佳模型"
            - 操作: (界面二)默认使用豆包模型, 界面二的:当前模型按钮上的状态要发生转变
        - b2: **速度优先** 
            - 描述："响应最快，适合简单问题"  
            - 操作: (界面二)使用豆包模型, 界面二的:当前模型按钮上的状态要发生转变
        - b3: **质量优先** 
            - 描述："回答质量最高，适合复杂问题"
            - 操作: (界面二)使用DeepSeek模型, 界面二的:当前模型按钮上的状态要发生转变
- c. **模型状态面板** (ModelStatus)
    - 显示当前可用模型状态：
        - 模型名称 + 状态指示器（在线/离线）



## 界面十: ConversationGraphScreen - 对话图谱界面

**功能**：对话关系可视化展示

**布局模块**：

- a. **返回导航**
    - 左上角返回箭头按钮
        - 点击后返回(界面三)
    - 标题："对话图谱"
- b. **图谱可视化区域** (GraphCanvas)
    - 全屏Canvas绘制关系图谱
    - 节点类型：
        - 用户消息节点
        - AI回复节点  
        - 关键概念节点
    - 连线表示关系关联
- c. **图谱工具栏** (GraphToolbar)
    - 悬浮在图谱上方：
        - c1: 缩放控制（放大🔍 + 缩小🔍）
        - c2: 重置视图按钮(🔄)
        - c3: 导出图片按钮(📷)
            - 点击后将其存储到系统相册中
- d. **节点信息面板** (NodeInfoPanel)
    - 点击节点后从底部滑出
    - 显示节点详细信息：
        - 节点内容
        - 关联节点
        - 时间信息
- e. **图谱图例** (GraphLegend)
    - 右下角悬浮图例
    - 说明不同颜色和形状的含义



## 交互状态说明

### 全局交互规范 

- (所有图标无论是AI生成的还是等待用户自行设置的都需要放置的位置必须清晰的展示)
- **加载状态**：统一的加载指示器
- **空状态**：友好的图标和引导文字
- **错误状态**：明确的错误信息和重试按钮
- **成功状态**：简洁的成功提示

### 特色功能集成

所有特色功能都通过侧边栏统一访问，确保用户体验的一致性，同时尽可能保持主界面的简洁性